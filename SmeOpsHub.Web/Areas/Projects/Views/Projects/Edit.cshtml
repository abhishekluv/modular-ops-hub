@model ProjectUpsertVm
@{
    ViewData["Title"] = "Edit Project";
    var id = (string?)ViewContext.RouteData.Values["id"];
}

<h2>Edit Project</h2>

<form method="post" asp-area="Projects" asp-controller="Projects" asp-action="Edit" asp-route-id="@id">
    @Html.AntiForgeryToken()

    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <partial name="_ProjectForm" model="Model" />

    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">Save</button>
        <a class="btn btn-outline-secondary" asp-area="Projects" asp-controller="Projects" asp-action="Details" asp-route-id="@id">
            Cancel
        </a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
